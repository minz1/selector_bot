# selector_bot

This bot, once you supply a server ID, voice channel ID, role ID, and a bot token,
will automatically give a user that joins the voice channel referenced by the ID
the role that was given by ID. The bot then removes the role should the user move
out into a different voice channel.
